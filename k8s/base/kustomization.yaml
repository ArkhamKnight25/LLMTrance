apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: llmtrace
resources:
  - namespace.yaml
  - configmap.yaml
  - postgres.yaml
  - redis.yaml
  - api.yaml
  - worker.yaml
  - web.yaml
  - ingress.yaml
# secrets.yaml is intentionally omitted — apply separately (gitignored)
