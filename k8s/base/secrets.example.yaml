# Copy to secrets.yaml (gitignored) and fill in real values, then:
#   kubectl apply -f k8s/base/secrets.yaml
#
# OR use sealed-secrets / external-secrets / sops in production.
apiVersion: v1
kind: Secret
metadata:
  name: llmtrace-secrets
  namespace: llmtrace
type: Opaque
stringData:
  OPENAI_API_KEY: "sk-..."
  ANTHROPIC_API_KEY: ""
  INGESTION_API_KEY: "change-me-strong-random-string"
